const Response = {
  SUCCESS: 'success',
  ERROR: 'error',
};

const API_RESPONSE = {
  NEW_USER_ADDED: 'new_user_added',
};

// eslint-disable-next-line import/prefer-default-export
module.exports = { Response, API_RESPONSE };

import {StyleSheet} from 'react-native';
import {COLORS} from '../../../../assets/theme';

export default StyleSheet.create({
  searchContainer: {
    margin: 10,
    width: '100%',
    alignSelf: 'center',
  },
});
